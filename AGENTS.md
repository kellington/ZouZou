# AGENTS.md

All project documentation and agent instructions live in **CLAUDE.md**.

Do not add content here unless it is specific to a non-Claude tool (Codex,
Cursor, Aider, etc.) and would be wrong for Claude Code to read.

```
CLAUDE.md          ← the protocol (edit this one)
└── AGENTS.md      ← this file: pointer for other agent tools
```

@CLAUDE.md

<!-- Prefer a hard link over a pointer? From the repo root:

       rm AGENTS.md && ln -s CLAUDE.md AGENTS.md

     Works with every tool and can never drift, but git stores it as a symlink —
     avoid it in a repo that syncs through iCloud or Dropbox. -->
