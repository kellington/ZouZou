# 2026-09


## 026-09-16
- migrating from Replit


Alice — /start-session. We're moving ZouZou off Replit to Cloudflare (Worker + static assets + D1).
Decisions are final: see plan §8.
Read first: ../../SKYideas/SKYresearch/experiments/ai-hosting/notes/zouzou-migration-plan-2026-09-16.md
and ../../SKYideas/SKYresearch/experiments/ai-hosting/notes/static-site-playbook-2026-09-16.md.


## 2026-09-17
- continuing conversion ...


rob@Robs-MacBook-Air ZouZou % npx wrangler d1 execute zouzou --remote --command "SELECT (SELECT COUNT(*) FROM player_games) games, (SELECT COUNT(DISTINCT name_key) FROM player_games) players, (SELECT COUNT(*) FROM player_games WHERE game='daily') daily_games, (SELECT COUNT(*) FROM daily_scores) daily_scores, (SELECT COUNT(DISTINCT date) FROM daily_scores) dates, (SELECT COUNT(*) FROM daily_scores WHERE date='2026-09-16') d0916, (SELECT COUNT(*) FROM player_games WHERE typeof(seconds)<>'integer')+(SELECT COUNT(*) FROM daily_scores WHERE typeof(seconds)<>'integer') nonint"

┌───────┬─────────┬─────────────┬──────────────┬───────┬───────┬────────┐
│ games │ players │ daily_games │ daily_scores │ dates │ d0916 │ nonint │
├───────┼─────────┼─────────────┼──────────────┼───────┼───────┼────────┤
│ 63    │ 5       │ 27          │ 26           │ 15    │ 2     │ 0      │
└───────┴─────────┴─────────────┴──────────────┴───────┴───────┴────────┘


**WORKING** https://zouzou.minus1over12.com

- Migration done: live at https://zouzou.minus1over12.com (Worker + D1); Replit unpublished; friends texted.
- Skipped final ReplDB export (live API matched backup); kept zz-test row in D1.

- Status page: first `/project-status` run → `project/status/status-2026-09-17.html` + STATUS-SUMMARY.md (uncommitted).


## 2026-09-17
- Brent doesn't like cats ...
Alice - One of the users thinks he would enjoy the game more if he was searching for something other than cats.  So my idea - could we have a settings option for each player that keeps a choice that we offer.  In addition to "cats", the user can look for "dogs", "dinosaurs", or "monkeys"




### Next Prompt

```text
Alice — /start-session. ZouZou is live on Cloudflare (Worker + D1) since 2026-09-17; Replit is unpublished.
1. Commit project/status/* + STATE/TASKS/DECISIONS/diary on `cf`, then PR `cf` → main (ask me first; README is already in 33eb7b6).
2. Milestone checkpoint: "Off Replit" is done — rewrite PLAN.md for "Settle in", prune TASKS.md,
   re-check PROJECT.md success criteria.
3. If it's ~2026-09-24 or later: walk me through the one-week Cloudflare usage check
   (Worker requests, D1 rows read/written, rate-limit hits vs Free limits) and draft the
   SKYresearch §11.8–11.10 updates.
```




Alice here. Quincy re-checked everything and says **ship**. No defects are left.

**What he confirmed:**
- **Picker:** at phone width (375px), the selected animal is clearly highlighted and the caption shows the right name. Nothing below it moves when you switch. A missing or unknown value shows exactly the same as Cats, and the arrow keys work.
- **Sounds:** the meow is unchanged. Each animal plays its own sound, anything unknown gets the meow, and a device with no audio support stays quiet without an error.
- **No regressions:** the 76 cookie tests pass, the build passes, and only client files changed.

**A correction to what I told you earlier:** the sound plays on **every correct placement**, not only when you win. That's how the meow already worked. So dog players hear a bark each time they place a dog. Tell me if you want it to play only on the win instead.

**For you:**
1. **Listen to the sounds** in the preview tab I opened, and tell me whether they're OK or which to redo.
2. **Phone checks:** run ` An`, then open `http://<your-Mac-LAN-IP>:8787` on your phone. Check that:
   - Tapping each animal highlights it, and it's still picked after you reload.
   - Clearing cookies brings back Cats.
   - Each animal's win title, icon and rules text match that animal.
   - The sound volume is OK next to the mistake buzz, with the iOS silent switch both on and off.
   - A named win shows up on the local leaderboard.
   - VoiceOver reads each animal name once.
3. **Commit:** say the word and I'll commit the 9 files on `feature/critters`. Then you push and open the PR to `main`, and merging deploys it.


## 2026-09-18
- bug fix when saving
- Rob's daily didn't reach the Top 5 / Recent players. Cause: saveBestTime sets lastDailyDate on the
  winning move → Game.tsx swaps to "Daily puzzle complete", hiding the win popup (name box + Top 5).
  Named players still saved; unnamed ones (everyone new to the domain — cookie is per-origin) saved nothing.
  Fix: skip that screen while gameState === 'won' (658571d, PR #6). Deployed, Rob verified.
- Rob's lost 09-18 score not backfilled — he'll play again tomorrow.

### Next Prompt

```text
Alice — /start-session. ZouZou live on Cloudflare; critter picker + daily-save fix shipped 2026-09-18.
1. Commit STATE/TASKS/diary, pull main, delete merged feature/critters (ask me first).
2. Milestone checkpoint: "Off Replit" is done — rewrite PLAN.md for "Settle in", prune TASKS.md,
   re-check PROJECT.md success criteria.
3. If it's ~2026-09-24 or later: one-week Cloudflare usage check → SKYresearch §11.8–11.10.
```
